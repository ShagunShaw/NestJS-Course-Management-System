services:
  mongo:
    image: mongo:4.4
    ports: 
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:

# To start this run 'docker compose up -d'
# understand this from gpt and timestamp 1.10.00
# also understand how this actually work, do we create a database locally or use it from the cloud.